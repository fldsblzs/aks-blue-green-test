---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp-blue
  namespace: bluegreentest
spec:
  replicas: 3
  selector:
    matchLabels:
      app: testapp
      color: blue
  template:
    metadata:
      labels:
        app: testapp
        color: blue
    spec:
      containers:
      - name: testapp-blue
        image: fblzs/bluegreenapp:bluev2
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Test"
        - name: ASPNETCORE_URLS
          value: "http://+;"
        livenessProbe:
          httpGet:
            path: /self
            port: 80
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /ready
            port: 80
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 15
