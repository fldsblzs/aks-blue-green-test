---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: testapp-{{{DEPLOYMENT}}}
  namespace: bluegreentest
spec:
  replicas: 3
  selector:
    matchLabels:
      app: testapp
      color: {{{DEPLOYMENT}}}
  template:
    metadata:
      labels:
        app: testapp
        color: {{{DEPLOYMENT}}}
    spec:
      containers:
      - name: testapp-{{{DEPLOYMENT}}}
        image: fblzs/bluegreenapp:{{{DEPLOYMENT}}}v2
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Test"
        - name: ASPNETCORE_URLS
          value: "http://+;"
        livenessProbe:
          httpGet:
            path: /self
            port: 80
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /ready
            port: 80
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 15